version: '2'
services:
  api:
    image: ${REGISTRY_URL}/stockportcontentapi:${TAG}
    ports:
      - 8080:5001
    environment:
      - http_proxy
      - https_proxy
      - no_proxy
      - VIRTUAL_HOST=*.dbdtest.stockport.gov.uk,content
      - CONTENTFUL_KEY
      - CONTENTFUL_SPACE
